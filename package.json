{
  "name": "notification-lambda",
  "version": "1.0.0",
  "description": "this lambda is used to send notification to phone number",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jinal",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
